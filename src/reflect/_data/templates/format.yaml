# reflect format configuration
# Seed categories for the knowledge base.
# The ingest triage agent can create additional categories dynamically
# based on what it finds in session evidence.
# Edit section names and purposes to customize for your project.

sections:
  - name: Key Decisions & Rationale
    purpose: why things are the way they are, not what they are
    max_bullets: 8
    recency: 30d

  - name: Gotchas & Friction
    purpose: things that burned time or surprised the agent
    max_bullets: 6
    recency: 14d

  - name: Open Work
    purpose: unfinished items a new session should pick up
    max_bullets: 5
    recency: 7d

  - name: Critical Pitfalls
    purpose: agent mistakes, reverted work, and failed approaches — each entry is a DON'T rule backed by evidence of what went wrong
    max_bullets: 8
    recency: 90d
    entry_fields:
      - mistake         # what the agent did wrong
      - consequence     # what broke or had to be reverted
      - rule            # the "don't do X because Y" directive for future agents

  - name: Patterns & Conventions
    purpose: coding patterns, naming conventions, and style preferences the team follows
    max_bullets: 10
    recency: 90d

  - name: Preferences
    purpose: user and team preferences — tools, libraries, approaches, style choices
    max_bullets: 10
    recency: 90d

citations: required
max_lines: 150
